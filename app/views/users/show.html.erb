<header class="masthead">

<div class="container d-flex h-100 align-items-center">
  <div class="mx-auto text-center">


<% if current_user %>
<h1 class="mx-auto my-0 text-uppercase">Here is your profile!</h1>
<% elsif current_trainer %>
<h1 class="mx-auto my-0 text-uppercase">Clients Profile:</h1>
<% end %>
<h2 class="mx-auto my-0 text-uppercase">Name: <%= @user.name %></h2>
<h2 class="mx-auto my-0 text-uppercase">Age: <%= @user.age %></h2>
<h2 class="mx-auto my-0 text-uppercase">Gender: <%= @user.gender %></h2>
<h2 class="mx-auto my-0 text-uppercase">Area: <%= @user.area.name %></h2>


<% if current_user %>
 <h4 class="mx-auto my-0 text-uppercase"><%= link_to("Edit My Information", edit_user_path) %></h4>
 <h4 class="mx-auto my-0 text-uppercase"><%= link_to "Delete My Account", user_path(@user.id), method: :delete, data: {confirm: "Are you sure you want to delete your account?"} %></h4>


 <h2 class="mx-auto my-0 text-uppercase">Trainers in my area: </h1><% @user.trainers_in_my_area.each do |trainer| %>
  <h2 class="mx-auto my-0 text-uppercase"><li><%= link_to trainer.name, trainer_path(trainer.id) %></li></h2>
     <% end %>

     <h3 class="mx-auto my-0 text-uppercase">Click on a trainer name to view their profile!</h3>

     <h2 class="mx-auto my-0 text-uppercase"><%= link_to("Book a session with a trainer in your area", new_booking_path) %></h2>

   <% if @user.bookings != [] %>
    <h1 class="mx-auto my-0 text-uppercase">Your Bookings:</h1>
     <p class="mx-auto my-0 text-uppercase">If you cancel your appointment over 24 hours in advance of the session time, you will be refunded. If you cancel you booking less than 24 hours before your session, your will forfeit your deposit.
     Within 24 hours of making a booking, the selected trainer should email you to organise location and payment.</p>
   <div>
     <% @user.bookings.each do |booking| %>
      <h2 class="mx-auto my-0 text-uppercase"><li>You are booked with <%= booking.trainer.name %> on <%= booking.date.strftime('%a %d %b %Y') %>, at <%= booking.time %>. Their email is: <%= booking.trainer.email %>. <%= link_to "Delete Booking", booking_path(booking.id), method: :delete, data: {confirm: "Are you sure you want to delete this booking?"} %></li></h2><br>
     <% end %>
     </div>
    <% else %>
    <h2 class="mx-auto my-0 text-uppercase">You do not have any bookings yet.</h2>
    <% end %>

    <% end %>


</div>
</header>

<!-- About Section -->
<div>
<img src="../../assets/man_hanging.jpg" class="img-fluid header_bg" alt="">
<section id="about" class="about-section text-center">


  
</div>
</section>


<!-- Bootstrap core JavaScript -->
<script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- Plugin JavaScript -->
<script src="vendor/jquery-easing/jquery.easing.min.js"></script>

<!-- Custom scripts for this template -->
<script src="js/grayscale.min.js"></script>